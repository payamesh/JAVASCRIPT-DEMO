Fråga 1.
Svar: Att Javascript kallas för High-level språk är pga språket ligger långt ifrån maskinnivå.
Js är ett interpreterat programmeringsspråk betyder att det inte kompileras utan körs direkt i webbläsaren via en javascript motor(v8).

Fråga 2:
JS märker av vilken sorts typ variabeln är utan att man själv behöver definiera det.
JS lägger även till ett semikolon via ASI(automatic semicolon insertion).
Du kan även byta datatyper lätt via javascript vilket inte går att göra lika enkelt med andra programmeringspråk.

Fråga 3:
console.log() skriver ut ett meddelande via consolen. Console är ett object och log är en metod som skriver ut det som är inom paranteserna.
Andra användbara console-metoder:
console.timer() - startar en timer 
console.timerend() - stoppar timerend
Detta kan man använda för att mäta hur lång tid det tar att köra ett program.

Fråga 4:
Att deklarera en variabel är att skriva tex let x = ;
att assign en variabel blir då att skriva x= 1; för att ge variabeln värdet 1.

Fråga 5:
Dem som inte går att använda är följande:
let - let är ett reserverat namn. använder man detta så kan det bli fel vid initiering av andra variabler av typen let.
% - är ett verktyg för att dela på och få remaindern av ett tal. tex 12%6 = 0.
MAX-NUMBER - eftersom javascript ser -:tecken som MINUS så kommer JS försöka subtrahera max med number, vilket inte går eftersom dem bägge är strängar.
middle-name - som ovan.
last.name - här blir det fel, då JS inte förväntar sig ett punkt mitt i deklarationen och ge oss ett felmeddelande som heter Unexpected token.

Fråga 6:
null är en datatyp som betyder "nothing" - ingenting alltså.
undefined är en datatyp som betyder att variabeln är en tom sträng.

Fråga 7:
NaN är en egenskap som betyder värdet som hade en siffra, inte längre är en siffra (Not a Number)

Fråga 8:
Semicolon används för att visa att en kodsats slutar.
Om man skriver en kodsats på endast en rad så behöver man inte använda det, däremot så ser det bättre ut.
om man skriver flera kodsatser på en rad så behöver man lägga ett semicolon mellan varje kodsats.
om man använder Strict mode så behöver man lägga semicolon efter varje rad;

exempel: 
let x = 1  //ok
let x = 1; //ok
let x=1 let y=2 //inte ok
let x=1; let y=2; // ok

Fråga 9.
aritmetiska metoder är det man använder för att arbeta med siffror/variabler, 
addera,subtrahera, dividera,multiplicera

exempel:
let x = 1;
let y= 2;
console.log(x+y);
console.log(x-y);
etc.

Fråga 10:
Arrayen har plats för två element. När vi skriver ut length får vi ut siffran 2.
eftersom javascript börjar med 0 i indexeringen, och vi skapar nya variabeln på siffran 2 så får 
volvo plats 3 som är [2] i arrayen.






